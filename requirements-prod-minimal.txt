# ULTRA-MINIMAL Production dependencies for Autoscale (<1GB)
# Removes ALL heavy OCR/PDF processing packages
# System relies ONLY on OCR.space API (cloud) for document processing

Django==5.0.6
gunicorn
python-dotenv
openpyxl
Pillow
PyPDF2
pytesseract
requests

# Groq LLM for text extraction (lightweight)
# Note: Using requests instead of openai/google-genai to reduce dependencies

# REMOVED for deployment (too heavy):
# - opencv-python-headless (50MB+)
# - pdfplumber (dependencies too heavy)
# - camelot-py (requires opencv, pandas, numpy = 200MB+)
# - pdf2image (poppler dependency)
# - openai/google-genai (large dependency trees)

# Production Strategy:
# 1. OCR.space API for all OCR (25k free/month)
# 2. Groq API via requests library (lightweight)
# 3. Tesseract OCR via Nix (system package)
